# This is an example of what a secrets file looks like
# Noermally it would just be named 'secrets.yaml'

# Print which file secrets are retrieved from
# (doesn't reveal secrets themselves)

# Useful for debugging secrets issues
logger: debug

# Use this file to store secrets like usernames and passwords.
# Learn more at https://www.home-assistant.io/docs/configuration/secrets/
some_password: welcome

my_secret_credential: "password"
